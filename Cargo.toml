[package]
name = "hasher"
version = "0.1.0"
edition = "2024"

[[bin]]
name = "hasher"
path = "src/main.rs"

[lib]
name = "hasher"
path = "src/lib.rs"

[dependencies]
clap = { version = "4.5.32", features = ["derive"] }


[profile.release]
opt-level = 3
lto = "fat"
codegen-units = 1
panic = "abort"
